export interface queryValidator {
    schedule_id: string;
    course_id: string;
    course_room: string;
    course_name: string;
    faculty: string;
    time_start: string,
    time_end: string,
}